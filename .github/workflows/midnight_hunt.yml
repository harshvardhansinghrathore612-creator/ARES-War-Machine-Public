name: ARES V60 Absolute Finality
on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  hunt:
    runs-on: ubuntu-latest
    timeout-minutes: 360 
    permissions:
      contents: write
      actions: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. TOTAL SYSTEM PURGE: No more fighting with pre-installed Go
      - name: Maximize Disk Space & Purge
        run: |
          sudo rm -rf /usr/local/go /usr/bin/go /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/az /opt/hostedtoolcache
          sudo apt-get clean

      # 2. HARDCODED GO INSTALL: Fixes GOROOT & awk issues permanently
      - name: Force Official Go Installation
        run: |
          wget https://go.dev/dl/go1.22.0.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf go1.22.0.linux-amd64.tar.gz
          echo "/usr/local/go/bin" >> $GITHUB_PATH
          echo "/home/runner/go/bin" >> $GITHUB_PATH
          echo "GOROOT=/usr/local/go" >> $GITHUB_ENV
          echo "GOPATH=/home/runner/go" >> $GITHUB_ENV

      # 3. CORE 4 FORCE: Manual install of the tools that MUST exist
      - name: Force Master Tools
        run: |
          export PATH=$PATH:/usr/local/go/bin
          go install -v github.com/tomnomnom/anew@latest
          go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
          go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
          go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
          sudo cp /home/runner/go/bin/* /usr/bin/

      - name: Official reconFTW Installation
        run: |
          git clone --depth 1 https://github.com/six2dez/reconftw
          cd reconftw/ && chmod +x install.sh
          yes | ./install.sh --verbose

      # 4. THE BINARY WELD: Making everything visible to 'sudo'
      - name: System Binary Weld
        run: |
          sudo ln -sf /usr/local/go/bin/* /usr/bin/ || true
          sudo ln -sf /home/runner/go/bin/* /usr/bin/ || true
          sudo ln -sf /home/runner/.local/bin/* /usr/bin/ || true
          sudo find /home/runner/Tools -executable -type f -exec ln -sf {} /usr/bin/ \; || true
          mkdir -p /home/runner/.cache /home/runner/.config
          sudo ln -sf /home/runner/.cache /.cache
          sudo ln -sf /home/runner/.config /.config
          sudo chmod -R 777 /home/runner

      # 5. EXECUTION: The .y Bug Fix & Attack
      - name: Execute Full Attack Suite
        timeout-minutes: 310
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_PAT }}
          HOME: /home/runner
        run: |
          # The 'tr' command kills hidden characters that cause the ".y" bug
          TARGET=$(cat targets.txt | tr -d '\r' | xargs)
          export PATH="/usr/local/go/bin:/home/runner/go/bin:/home/runner/.local/bin:/usr/bin:/bin:$PATH"
          cd reconftw/
          
          # Telegram Start Notification
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=🔥 [ARES V60] ATTACK STARTING: $TARGET"
          
          # Force Nuclei Templates & Disable Probing
          nuclei -ut || true
          sed -i 's/WEBPROBEFULL=true/WEBPROBEFULL=false/g' reconftw.cfg
          sed -i 's/WEBPROBESIMPLE=true/WEBPROBESIMPLE=false/g' reconftw.cfg
          
          sudo -E PATH=$PATH ./reconftw.sh -d $TARGET -r -a -s --deep
          
      # 6. SPLIT-ZIPPER: Telegram 50MB Bypass
      - name: Pack & Send Master Report
        if: always() 
        run: |
          TARGET=$(cat targets.txt | tr -d '\r' | xargs)
          if [ -d "reconftw/Recon/$TARGET" ]; then
            sudo zip -r "ARES_REPORT.zip" "reconftw/Recon/$TARGET"
            split -b 45M "ARES_REPORT.zip" "ARES_PART."
            for part in ARES_PART.*; do
              curl -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                   -F document=@"$part" \
                   -F caption="📦 Part $part for $TARGET" \
                   "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendDocument"
              sleep 2
            done
          else
             curl -s -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                  -F text="❌ FAIL: No results for $TARGET." \
                  "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage"
          fi
          
