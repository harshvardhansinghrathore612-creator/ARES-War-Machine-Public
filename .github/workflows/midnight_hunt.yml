name: ARES V52 Last Chance
on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 

jobs:
  hunt:
    runs-on: ubuntu-latest
    timeout-minutes: 360 
    permissions:
      contents: write
      actions: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. DISK PURGE: Max room for the 10GB+ official toolset
      - name: Maximize Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost /usr/local/go /usr/lib/jvm /usr/share/swift /usr/local/.ghcup /opt/az /usr/local/share/powershell /opt/hostedtoolcache
          sudo apt-get clean

      # 2. PATH LOCKDOWN: Forces the system to remember tool locations globally
      - name: Global Environment Lock
        run: |
          echo "/home/runner/go/bin" >> $GITHUB_PATH
          echo "/home/runner/.local/bin" >> $GITHUB_PATH
          echo "/home/runner/Tools" >> $GITHUB_PATH
          echo "GOPATH=/home/runner/go" >> $GITHUB_ENV

      # 3. OFFICIAL INSTALLER: Forced Non-Interactive Mode
      - name: Official Installation
        run: |
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update && sudo apt-get install -y golang-go libpcap-dev libcurl4-openssl-dev libxml2-dev libxslt1-dev python3-pip python3-dev zip pipx
          git clone --depth 1 https://github.com/six2dez/reconftw
          cd reconftw/
          chmod +x install.sh
          # The 'yes' command bypasses all installer prompts
          yes | ./install.sh --verbose

      # 4. THE WELD: Hard-linking tools to /usr/bin so the scanner CANNOT miss them
      - name: System Binary Weld
        run: |
          sudo ln -sf /home/runner/go/bin/* /usr/bin/ || true
          sudo ln -sf /home/runner/.local/bin/* /usr/bin/ || true
          # Recurse into the Tools folder to find every hidden binary
          sudo find /home/runner/Tools -executable -type f -exec ln -sf {} /usr/bin/ \; || true
          
          # Fix Permission Denied for Nuclei and others
          mkdir -p /home/runner/.cache /home/runner/.config
          sudo ln -sf /home/runner/.cache /.cache
          sudo ln -sf /home/runner/.config /.config
          sudo chmod -R 777 /home/runner

      # 5. EXECUTION: Official flow with Probing Disabled
      - name: Execute Full Attack Suite
        timeout-minutes: 310
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_PAT }}
          HOME: /home/runner
        run: |
          TARGET=$(cat targets.txt)
          cd reconftw/
          
          # Force create missing Nuclei directories
          nuclei -update-templates || true
          
          # Disable Probing as requested
          sed -i 's/WEBPROBEFULL=true/WEBPROBEFULL=false/g' reconftw.cfg
          sed -i 's/WEBPROBESIMPLE=true/WEBPROBESIMPLE=false/g' reconftw.cfg
          
          # RUN: Forces the PATH even under SUDO
          sudo -E PATH=$PATH ./reconftw.sh -d $TARGET -r -a -s --deep
          
      # 6. SPLIT-ZIPPER: Bypasses Telegram 50MB Limit
      - name: Pack & Send Master Report
        if: always() 
        run: |
          TARGET=$(cat targets.txt)
          if [ -d "reconftw/Recon/$TARGET" ]; then
            sudo zip -r "ARES_FINAL_REPORT.zip" "reconftw/Recon/$TARGET"
            split -b 45M "ARES_FINAL_REPORT.zip" "ARES_PART."
            for part in ARES_PART.*; do
              curl -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                   -F document=@"$part" \
                   -F caption="📦 Part $part for $TARGET" \
                   "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendDocument"
              sleep 2
            done
          else
             curl -s -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                  -F text="❌ CRITICAL FAILURE: Results folder not found. Check install logs for tool errors." \
                  "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage"
          fi
          
